import {createClient} from "../../lib/supabase/server";import SuperAdmin from "../../components/SuperAdmin";export const dynamic="force-dynamic";export default async function Admin(){const s=await createClient();const[{data:stores},{data:brands},{data:sources},{data:products},{data:qrBatches}]=await Promise.all([s.from("stores").select("id,name,slug,city,is_published,created_at").order("name"),s.from("brands").select("id,name,slug,logo_url,website_url").order("name"),s.from("brand_sources").select("*").order("created_at",{ascending:false}),s.from("products").select("id,brand_id,name,model_year,is_active"),s.rpc("admin_list_qr_batches")]);return <SuperAdmin stores={stores??[]} brands={brands??[]} sources={sources??[]} products={products??[]} qrBatches={qrBatches??[]}/>} 