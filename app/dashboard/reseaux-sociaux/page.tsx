import SocialManager from "../../../components/SocialManager";import {getDemoStore} from "../../../lib/data";import {createClient} from "../../../lib/supabase/server";export const dynamic="force-dynamic";
export default async function SocialPage(){const store=await getDemoStore();const s=await createClient();const[{data:brands},{data:posts},{data:products}]=await Promise.all([s.from("store_brands").select("brand_id,brands(id,name,logo_url)").eq("store_id",store.id),s.from("social_posts").select("*,brands(name,logo_url)").order("created_at",{ascending:false}),s.from("products").select("id,name,model_year,brand_id,brands(name)").eq("is_active",true).order("name")]);return <><header className="dashHeader"><div><p className="dashEyebrow">RÉSEAUX SOCIAUX</p><h1>Ton Community Manager Bikéo</h1><p>Les contenus de tes marques et tes actualités magasin, préparés pour tes réseaux.</p></div></header><SocialManager store={store} brands={brands??[]} posts={posts??[]} products={products??[]}/></>}