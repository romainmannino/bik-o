import {getDemoStore,getStoreBrands} from "../../../lib/data";import StoreSiteForm from "../../../components/StoreSiteForm";export const dynamic="force-dynamic";export default async function SiteEditor(){const store=await getDemoStore();const brands=await getStoreBrands(store.slug);return <><header className="dashHeader"><div><p className="dashEyebrow">MON SITE</p><h1>Personnaliser mon site</h1><p>Modifie les textes directement sur le site. Le design et les médias restent dans le panneau de gauche.</p></div></header><StoreSiteForm store={store} brands={brands}/></>}