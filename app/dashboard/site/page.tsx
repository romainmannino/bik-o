import {getDemoStore} from "../../../lib/data";import StoreSiteForm from "../../../components/StoreSiteForm";export const dynamic="force-dynamic";export default async function SiteEditor(){const store=await getDemoStore();return <><header className="dashHeader"><div><p className="dashEyebrow">MON SITE</p><h1>Personnaliser mon site</h1><p>Tu personnalises l'identité et le contenu. Bikéo garde la structure du site.</p></div><a className="primaryBtn previewLink" href={"/magasin/"+store.slug} target="_blank">Voir le site ↗</a></header><StoreSiteForm store={store}/></>}