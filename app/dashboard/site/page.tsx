import {getDemoStore} from "../../../lib/data";import StoreSiteForm from "../../../components/StoreSiteForm";export const dynamic="force-dynamic";export default async function SiteEditor(){const store=await getDemoStore();return <><header className="dashHeader"><div><p className="dashEyebrow">MON SITE</p><h1>Personnaliser mon site</h1><p>Tu personnalises ton identité et tes contenus. Bikéo garde une structure efficace et cohérente.</p></div></header><StoreSiteForm store={store}/></>}