import {getCatalog} from "../../../lib/data";import CatalogueFilters from "../../../components/CatalogueFilters";export const dynamic="force-dynamic";
const euro=(c:number|null)=>c==null?"—":new Intl.NumberFormat("fr-FR",{style:"currency",currency:"EUR",maximumFractionDigits:0}).format(c/100);
export default async function Catalogue(){const raw:any[]=await getCatalog();const bikes=raw.map(b=>{const inv=(b.product_variants??[]).flatMap((v:any)=>v.inventory??[]);const stock=inv.some((i:any)=>i.public_status==="in_stock"&&i.quantity>0);const orderable=inv.some((i:any)=>i.public_status==="orderable");const statusKey=stock?"in_stock":orderable?"orderable":"unavailable";return{id:b.id,slug:b.slug,brand:b.brands?.name??"",name:b.name,category:b.category??"",motor:b.motor_system??"",price:euro(b.msrp_cents),status:stock?"En stock":orderable?"Sur commande":"Indisponible",statusKey}});return <><header className="dashHeader"><div><p className="dashEyebrow">CATALOGUE COMMUN</p><h1>Mes vélos</h1><p>Recherche et disponibilité viennent maintenant du catalogue réellement relié au magasin.</p></div></header><CatalogueFilters bikes={bikes}/></>}
