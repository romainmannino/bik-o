import { getDemoStore,getStoreStats } from "../../lib/data";import {createClient} from "../../lib/supabase/server";
export const dynamic="force-dynamic";
export default async function Dashboard(){const supabase=await createClient();const [store,stats]=await Promise.all([getDemoStore(),getStoreStats()]);const {count:leadCount}=await supabase.from("leads").select("*",{count:"exact",head:true}).eq("store_id",store.id);const cards=[["Vélos en catalogue",String(stats.products),"donnée catalogue"],["En stock",String(stats.inStock),"unités magasin"],["Leads clients",String(leadCount??0),"demandes enregistrées"],["Site magasin",store.is_published?"Publié":"Brouillon","état réel"]];return <><header className="dashHeader"><div><p className="dashEyebrow">BIENVENUE CHEZ BIKÉO</p><h1>Bonjour {store.name}.</h1><p>Voilà ce qui se passe aujourd'hui dans ton magasin digital.</p></div></header><section className="statGrid">{cards.map(([a,b,c])=><div className="statCard" key={a}><span>{a}</span><strong>{b}</strong><small>{c}</small></div>)}</section><section className="dashColumns"><div className="panel"><div className="panelTitle"><div><small>DONNÉES RÉELLES</small><h2>Ton magasin aujourd'hui</h2></div></div><div className="task"><i>1</i><div><strong>{leadCount??0} leads enregistrés</strong><p>Demandes site et Advisor réunies dans le même CRM.</p></div><b>→</b></div><div className="task"><i>2</i><div><strong>{stats.inStock} vélos disponibles immédiatement</strong><p>Calculé depuis inventory.</p></div><b>→</b></div><div className="task"><i>3</i><div><strong>{stats.products} modèles actifs</strong><p>Catalogue commun relié aux marques du magasin.</p></div><b>→</b></div></div><div className="panel darkPanel"><small>CONSEILLER</small><h2>Un client est devant toi ?</h2><p>L'Advisor utilise maintenant le catalogue et le stock puis transmet le contexte dans le lead.</p><a href="/conseiller">Conseiller un client →</a></div></section></>}
