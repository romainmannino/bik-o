import Link from "next/link";import Advisor from "../../components/Advisor";import {getCatalog} from "../../lib/data";
export const dynamic="force-dynamic";
export default async function Conseiller(){const raw:any[]=await getCatalog();const bikes=raw.map(b=>{const inv=(b.product_variants??[]).flatMap((v:any)=>v.inventory??[]);const stock=inv.find((i:any)=>i.public_status==="in_stock"&&i.quantity>0);const orderable=inv.find((i:any)=>i.public_status==="orderable");return {id:b.id,slug:b.slug,name:b.name,brand:b.brands?.name??"",category:b.category,motor:b.motor_system,price:stock?.price_cents??b.msrp_cents,status:stock?"in_stock":orderable?"orderable":"unavailable",qty:stock?.quantity??0}});return <main className="advisor"><Link href="/dashboard" className="back">← Espace magasin</Link><div className="advisorLogo">bik<span>é</span>o <small>ADVISOR</small></div><Advisor bikes={bikes}/></main>}
