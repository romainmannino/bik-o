"use client";import {useMemo,useState} from "react";import Link from "next/link";export default function PublicCatalogueFilters({bikes,storeSlug}:{bikes:any[],storeSlug:string}){const[q,setQ]=useState(""),[brand,setBrand]=useState("Toutes"),[year,setYear]=useState("Toutes"),[cat,setCat]=useState("Toutes"),[max,setMax]=useState("");const brands=["Toutes",...new Set(bikes.map(b=>b.brand).filter(Boolean))],years=["Toutes",...new Set(bikes.map(b=>String(b.year)).filter(Boolean))],cats=["Toutes",...new Set(bikes.map(b=>b.category).filter(Boolean))];const list=useMemo(()=>bikes.filter(b=>(!q||(b.brand+" "+b.name).toLowerCase().includes(q.toLowerCase()))&&(brand==="Toutes"||b.brand===brand)&&(year==="Toutes"||String(b.year)===year)&&(cat==="Toutes"||b.category===cat)&&(!max||!b.price||b.price<=Number(max)*100)),[bikes,q,brand,year,cat,max]);const euro=(c:number)=>c?new Intl.NumberFormat("fr-FR",{style:"currency",currency:"EUR",maximumFractionDigits:0}).format(c/100):"Prix sur demande";return <section className="publicCatalogBody"><div className="publicFilters"><input placeholder="Rechercher un modèle…" value={q} onChange={e=>setQ(e.target.value)}/><select value={brand} onChange={e=>setBrand(e.target.value)}>{brands.map(x=><option key={x}>{x}</option>)}</select><select value={year} onChange={e=>setYear(e.target.value)}>{years.map(x=><option key={x}>{x}</option>)}</select><select value={cat} onChange={e=>setCat(e.target.value)}>{cats.map(x=><option key={x}>{x}</option>)}</select><input type="number" placeholder="Prix max €" value={max} onChange={e=>setMax(e.target.value)}/></div><p className="catalogCount">{list.length} modèle{list.length>1?"s":""}</p><div className="publicBikeGrid">{list.map(b=><Link href={"/magasin/"+storeSlug+"/velos/"+b.slug} className="publicBikeCard" key={b.id}><div className="publicBikeImage">{b.image?<img src={b.image} alt={b.name}/>:<span>PHOTO EPOS</span>}<em className={"stockBadge "+b.statusKey}>{b.status}</em></div><small>{b.brand} · {b.year}</small><h2>{b.name}</h2><p>{b.category}</p><strong>{euro(b.price)}</strong></Link>)}</div></section>}