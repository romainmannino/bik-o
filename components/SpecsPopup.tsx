"use client";import {useState} from "react";export default function SpecsPopup({specs,labels}:{specs:any[];labels:Record<string,string>}){const[open,setOpen]=useState(false);return <><button type="button" className="allSpecsTrigger" onClick={()=>setOpen(true)}>Voir tous les composants</button>{open&&<div className="allSpecsOverlay" onClick={()=>setOpen(false)}><div className="allSpecsWindow" role="dialog" aria-modal="true" onClick={e=>e.stopPropagation()}><button type="button" className="popupClose" aria-label="Fermer" onClick={()=>setOpen(false)}>×</button><h3>Tous les composants</h3><dl>{specs.map(([k,v]:any)=><div key={k}><dt>{labels[k]||k.replaceAll("_"," ")}</dt><dd>{String(v)}</dd></div>)}</dl></div></div>}</>}